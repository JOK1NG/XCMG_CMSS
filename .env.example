# 后端 API 前缀（Vite 代理或网关前缀，默认 /api）
VITE_API_BASE_URL=/api

# 列表/看板请求失败时是否使用本地示例数据：true / false（未设置时：开发环境默认 true，生产默认 false）
VITE_USE_LIST_FALLBACK=true

# 仅开发：跳过登录路由守卫，便于无后端时调试页面（生产勿用）
# VITE_DEV_BYPASS_AUTH=true
